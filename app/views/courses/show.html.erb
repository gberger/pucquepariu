<h1>
  <%= @course.abbreviation %> - <%= @course.name %>
  <%= if can? :edit, @course then link_to '<i class="fa fa-pencil"></i>'.html_safe, edit_course_path(@course), class: "btn btn-success" end %>
</h1>

<%= markdown @course.study_plan %>

